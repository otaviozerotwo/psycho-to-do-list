import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  input: {
    borderWidth: 0.4,
    borderRadius: 10,
    borderColor: '#6C7278',
    marginBottom: 16
  },
  label: {
    fontSize: 12,
    fontWeight: 'medium',
    color: '#6C7278',
    marginBottom: 2
  },
});

export default styles;